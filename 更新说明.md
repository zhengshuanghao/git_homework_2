# 更新说明 - 图片尺寸调整功能

## 版本更新

### 新增功能 ✨

**图片尺寸调整功能** - 在导出时允许用户调整图片尺寸

#### 主要特性：
1. **四种调整模式**：
   - **按百分比缩放**: 支持10%-500%的缩放比例
   - **按宽度缩放**: 指定目标宽度，高度自动按比例调整
   - **按高度缩放**: 指定目标高度，宽度自动按比例调整  
   - **自定义尺寸**: 可同时设置宽高，可选择是否保持宽高比

2. **用户界面改进**：
   - 在导出设置页面添加"图片尺寸调整"设置区域
   - 复选框控制功能启用/禁用
   - 单选按钮选择调整模式
   - 对应参数输入控件自动显示/隐藏

3. **智能处理**：
   - 尺寸限制在1-20000像素之间，防止异常值
   - 保持宽高比模式自动选择较小的缩放比例
   - 如果尺寸无变化，直接返回原图副本以提高效率

## 技术实现

### 修改的文件：

#### 1. `src/app.py`
- **新增UI控件**：图片尺寸调整设置区域
- **新增方法**：
  - `toggle_resize_controls()`: 控制尺寸调整控件的启用状态
  - `on_resize_mode_change()`: 根据调整模式显示对应的参数输入框
  - `get_resize_parameters()`: 获取当前的尺寸调整参数
  - `_enable_widget()` / `_disable_widget()`: 递归启用/禁用小部件
- **更新导出流程**：在应用水印前先进行尺寸调整

#### 2. `src/image_processor.py`
- **新增方法**：
  - `resize_image_advanced()`: 高级图像尺寸调整，支持多种模式

#### 3. 文档更新
- **README.md**: 更新功能说明
- **使用指南.md**: 添加详细的使用说明和最佳实践

### 新增测试文件：
- **test_resize_feature.py**: 功能测试示例

## 使用方法

### 基本步骤：
1. 导入要处理的图片
2. 设置水印（文本或图片）
3. 在"导出设置"页面勾选"启用尺寸调整"
4. 选择调整模式并设置参数：
   - **按百分比**: 输入缩放百分比（如50表示缩小到50%）
   - **按宽度**: 输入目标宽度像素值
   - **按高度**: 输入目标高度像素值
   - **自定义**: 输入宽度和高度，选择是否保持宽高比
5. 选择输出目录和其他导出设置
6. 点击"开始导出"

### 应用场景：
- **缩小文件大小**: 使用百分比缩放或指定较小尺寸
- **统一图片规格**: 使用按宽度/高度缩放
- **自定义输出尺寸**: 使用自定义模式精确控制
- **批量处理**: 一次性处理多张图片到相同尺寸

## 注意事项

### ✅ 推荐做法：
- 处理前先在预览中测试效果
- 百分比缩放适合整体等比缩放
- 按宽度/高度缩放自动保持宽高比
- 大批量处理时建议先用少量图片测试

### ⚠️ 注意事项：
- 图片放大可能会降低画质
- 极大的尺寸调整会影响处理速度
- 自定义模式不保持宽高比时可能使图片变形

## 兼容性

- 支持所有原有的图片格式（JPEG, PNG, BMP, TIFF）
- 与现有的水印功能完全兼容
- 不影响原有的导出设置功能
- 向后兼容，可选择性使用新功能

## 测试验证

运行测试文件验证功能：
```bash
python test_resize_feature.py
```

启动完整应用程序：
```bash
python main.py
```

---

**此更新完全向后兼容，不会影响现有功能的使用。新功能默认为禁用状态，用户可根据需要选择性启用。**
